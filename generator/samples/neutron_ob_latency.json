{
    "name": "test",
    "timeout": 100,
    "observer": {
        "type": "neutron",
        "properties": {
            "address": "http://localhost:8000",
            "query": "select * from test where value=9000",
            "time_column": "time",
            "metric": "latency"
        }
    }
}